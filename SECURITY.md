# Security

Report vulnerabilities privately through GitHub Security Advisories. Never include live API keys, access tokens, customer data, or request dumps containing credentials in an issue.

Credentials are read from environment bindings, never returned by status tools, and never written to disk. Hosted deployments should use Cloudflare Worker secrets.
